{
  "_behavioral_specs": {
    "_global": [
      {
        "text": "## Summary\nBuild a greenfield Vue 3 + NestJS + PostgreSQL app that serves a fully-public bookmark list/create UI plus an About page, with server-side validation (http/https only), first-load seeding, graceful DB-error handling, and a minimal seeded admin login — packaged as a single container serving both API and SPA under the `/e2e-lane-vue-nest/` ingress sub-path.\n\n## Scope\n### Files to modify\n- `Dockerfile` — replace nginx placeholder with a multi-stage build (build Vue → build Nest → Node runtime serving both on port 80). Deploy agent will otherwise overwrite; provide the correct one.\n- `README.md` — document run/build/env.\n\n### Files to create\n**Backend (`backend/`)**\n- `package.json`, `tsconfig.json`, `nest-cli.json` — NestJS project config.\n- `src/main.ts` — bootstrap; global `ValidationPipe` (whitelist), listen on `process.env.PORT || 80`, global exception filter.\n- `src/app.module.ts` — `ConfigModule`, `TypeOrmModule` (Postgres, `retryAttempts`, `autoLoadEntities`), `ServeStaticModule` (serves `frontend/dist` assets, `serveRoot` off / no SPA wildcard fallback), feature modules.\n- `src/bookmarks/bookmark.entity.ts` — `id` (uuid), `title`, `url`, `createdAt`.\n- `src/bookmarks/dto/create-bookmark.dto.ts` — `@IsNotEmpty` title; url `@IsNotEmpty` + `@IsUrl({require_protocol:true, protocols:['http','https']})` (rejects blank, malformed, and non-http/https schemes like `javascript:`).\n- `src/bookmarks/bookmarks.service.ts` — CRUD + idempotent seeding helper.\n- `src/bookmarks/bookmarks.controller.ts` — `GET /api/bookmarks`, `POST /api/bookmarks`.\n- `src/bookmarks/bookmarks.seeder.ts` — `OnModuleInit`: if table empty, insert 3 examples; wrapped in try/catch so a down DB never crashes boot.\n- `src/bookmarks/bookmarks.module.ts`.\n- `src/frontend/frontend.controller.ts` — explicit `GET` handlers for `/`, `/bookmarks`, `/bookmarks/new`, `/about`, `/admin/login` returning `index.html`; all other unmatched paths fall through to Nest's default 404.\n- `src/health/health.controller.ts` — `GET /api/health` (liveness), `GET /api/health/deep` (DB ping via `@nestjs/terminus`).\n- `src/admin/admin.controller.ts` + `admin.module.ts` + `jwt.strategy.ts` — `POST /api/admin/login` validating seeded admin (bcrypt) → JWT; JWT guard available for future admin routes (gates nothing public).\n- `src/admin/admin.seeder.ts` — seed admin from `ADMIN_EMAIL`/`ADMIN_PASSWORD` env (idempotent).\n- `src/common/filters/all-exceptions.filter.ts` — maps DB/connection errors to JSON `503` (`{error:'service_unavailable'}`) instead of unhandled crash.\n\n**Frontend (`frontend/`)**\n- `package.json`, `tsconfig.json`, `vite.config.ts` (`base: '/e2e-lane-vue-nest/'`, build outDir consumed by backend), `index.html`.\n- `src/main.ts`, `src/App.vue`.\n- `src/router/index.ts` — `createWebHistory('/e2e-lane-vue-nest/')`; routes for `/bookmarks`, `/bookmarks/new`, `/about`, `/admin/login`, and `:pathMatch(.*)*` → NotFound; each route carries `meta.flow` node for machine-verifiability. No route guards (fully public).\n- `src/api/client.ts` — base URL `import.meta.env.BASE_URL + 'api'`; wraps fetch, surfaces network/DB errors.\n- `src/views/BookmarksView.vue` — heading \"My Bookmarks\", rows (title + URL), prominent \"Add Bookmark\" button → `/bookmarks/new`; graceful error state on fetch failure.\n- `src/views/BookmarkNewView.vue` — Title + URL fields, \"Create\" button; client validation + displays server validation errors; on success redirect to `/bookmarks`.\n- `src/views/AboutView.vue` — static, heading \"About Bookmarks\".\n- `src/views/AdminLoginView.vue` — admin login form (posts to `/api/admin/login`).\n- `src/views/NotFoundView.vue` — client-side 404 view.\n\n### Dependencies\n- Backend: `@nestjs/{core,common,platform-express,config,typeorm,jwt,passport,terminus}`, `typeorm`, `pg`, `class-validator`, `class-transformer`, `@nestjs/serve-static`, `passport`, `passport-jwt`, `bcrypt`.\n- Frontend: `vue`, `vue-router`, `vite`, `@vitejs/plugin-vue`, `typescript`.\n- Runtime env (from `app-secrets`): `DATABASE_URL` (or `PG*`), `JWT_SECRET`, `ADMIN_EMAIL`, `ADMIN_PASSWORD`, `PORT`.\n\n### Integrations\n- PostgreSQL via TypeORM (`pg` driver) — primary datastore (declared in spec `## Deployments`). No other third-party services.\n\n### Assumptions\n- Stack = Vue 3 + NestJS + PostgreSQL, inferred from repo name `e2e-lane-vue-nest` and spec deployment.\n- Single container: NestJS serves API + built SPA on port 80 (matches service `targetPort: 80`); Dockerfile placeholder replaced.\n- Base path `/e2e-lane-vue-nest/` for Vite + Router (matches ingress prefix/rewrite).\n- `admin_only` auth baseline included (`/admin/login` + seeded admin + JWT) but gates no public route, honoring the \"app fully public / no 401/403\" NFR.\n- Unknown routes return real server 404 (explicit-route serving, no SPA catch-all fallback).\n- Seed data: 3 example bookmarks (e.g. Vue, NestJS, PostgreSQL docs).\n\n## Implementation Plan\n\n### Step 1: Scaffold monorepo\n**Files:** `frontend/`, `backend/` project configs.\n**Change:** Init NestJS backend and Vue 3 + Vite + TS frontend with the package/tsconfig files above; wire Vite `base` and outDir so backend serves `frontend/dist`.\n\n### Step 2: Backend data layer + seeding\n**Files:** `bookmark.entity.ts`, `bookmarks.service.ts`, `bookmarks.seeder.ts`, `app.module.ts`.\n**Change:** Define entity, TypeORM Postgres connection with `retryAttempts`, idempotent 3-row seeding on `OnModuleInit` wrapped in try/catch.\n\n### Step 3: Bookmarks API + validation\n**Files:** `create-bookmark.dto.ts`, `bookmarks.controller.ts`.\n**Change:** `GET`/`POST /api/bookmarks`; DTO enforces non-empty title/url, valid URL, http/https-only scheme; global `ValidationPipe` returns 400 with messages.\n\n### Step 4: Static serving + explicit routes + 404\n**Files:** `frontend.controller.ts`, `ServeStaticModule` config.\n**Change:** Serve `/assets` statically; explicit handlers return `index.html` for known SPA routes; unmatched → 404 (satisfies `/foo` → 404 while keeping deep links working).\n\n### Step 5: Health + graceful DB errors\n**Files:** `health.controller.ts`, `all-exceptions.filter.ts`.\n**Change:** `/api/health` + `/api/health/deep`; filter converts DB/connection failures to 503 JSON so list/create show error state instead of crashing.\n\n### Step 6: Admin auth baseline\n**Files:** `admin.controller.ts`, `jwt.strategy.ts`, `admin.seeder.ts`.\n**Change:** Seed admin (bcrypt), `POST /api/admin/login` → JWT. No guards on public routes.\n\n### Step 7: Frontend views + routing\n**Files:** `router/index.ts`, all `views/*.vue`, `api/client.ts`.\n**Change:** Build list/new/about/admin-login/404 views with `meta.flow` nodes; API client on base-path-aware URL; graceful error UI on fetch failures; redirect to `/bookmarks` after create.\n\n### Step 8: Dockerfile + deploy wiring\n**File:** `Dockerfile`.\n**Change:** Multi-stage: build frontend, build backend, Node runtime copying `backend/dist` + `frontend/dist` + prod deps; run NestJS on port 80.\n\n## Testing Strategy\n- Unit: DTO validation — reject empty title/url, `not-a-url`, `javascript:alert(1)`; accept `https://…`.\n- Integration/e2e: `GET /api/bookmarks` returns seeded 3 on fresh DB; `POST` valid → 201 + appears in list; invalid → 400; `/foo` → 404; `/about` → 200; public routes never 401/403.\n- Seeding: boot with empty DB seeds exactly 3 (idempotent on restart).\n- Failure: stop DB → list/create render error state, app stays up; `/api/health` still 200, `/api/health/deep` 503.\n- Manual: verify deep-link `/e2e-lane-vue-nest/bookmarks` loads assets correctly through ingress; p95 render < 500ms under normal load.\n\n## Risks\n- Sub-path base misconfig would break asset loading — Vite `base` and Router base must both equal `/e2e-lane-vue-nest/`.\n- SPA fallback vs. 404: must use explicit-route serving, not catch-all, or `/foo` wrongly returns 200.\n- Port 80 binding requires container root (default) — keep as-is; if runtime is non-root, set `PORT` + service `targetPort`.\n- Seeding/queries must never crash boot when DB is down — enforce try/catch + `retryAttempts`.",
        "type": "architectural",
        "source": "plan",
        "rev": 0,
        "superseded": false
      }
    ]
  },
  "_component_requirements": {}
}